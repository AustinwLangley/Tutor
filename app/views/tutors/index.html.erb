<h1>Tutors</h1>
<% if logged_in? %>
  Logged in as: <%= current_tutor.name %> 

	<ul>
		<% @tutors.each do |tutor| %> 
		<li><%= tutor.name %></li>
	<% end %>
	</ul>

  <%= link_to 'Log Out', logout_path, method: :delete %>
<% else %>
  <%= link_to 'Log In', login_path %>
<% end %>